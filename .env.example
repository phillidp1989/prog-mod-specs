# Supabase Configuration (REQUIRED)
# These are required for the application to start
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Environment (development allows all CORS origins, production requires explicit whitelist)
NODE_ENV=development

# Allowed CORS origins (comma-separated, only enforced in production)
# Add your other application's URL here if deploying to production
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000,http://localhost:5173,http://127.0.0.1:8080,http://127.0.0.1:3000

# Gmail SMTP Configuration (OPTIONAL)
# The app will work without these, but email notifications will be disabled
# IMPORTANT: Keep your credentials secret! Never commit them to version control.
#
# Setup Instructions:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Create a new App Password (select "Mail" and your device)
# 4. Copy the 16-character password (without spaces) and use it as GMAIL_APP_PASSWORD
# 5. Use your full Gmail address as GMAIL_USER
#
# NOTE: Do NOT use your regular Gmail password - it won't work!
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password
NOTIFICATION_EMAIL=recipient@example.com
